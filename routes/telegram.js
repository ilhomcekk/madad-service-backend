const express = require("express");
const router = express.Router();
const telegram = require("../controllers/telegramController");

// Send message
// router.post("/telegram/create", telegram.telegram_create);

module.exports = router;
